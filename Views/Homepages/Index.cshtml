@model IEnumerable<Homepage>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create" class="btn btn-primary">Create New</a>
</p>
<table class="table table-bordered">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Slidertitle1)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Slidersubtitle1)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Sliderimage1)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Slidertitle2)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Slidersubtitle2)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Sliderimage2)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Lastupdated)
            </th>
            <th>
                Admin ID
            </th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Slidertitle1)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Slidersubtitle1)
                </td>
                <td>
                    <img src="@item.Sliderimage1" alt="Slider Image 1" style="max-width: 100px; max-height: 100px;" />
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Slidertitle2)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Slidersubtitle2)
                </td>
                <td>
                    <img src="@item.Sliderimage2" alt="Slider Image 2" style="max-width: 100px; max-height: 100px;" />
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Lastupdated)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Adminid)
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.HModificationid" class="btn btn-warning btn-sm">Edit</a>
                    <a asp-action="Details" asp-route-id="@item.HModificationid" class="btn btn-info btn-sm">Details</a>
                    <a asp-action="Delete" asp-route-id="@item.HModificationid" class="btn btn-danger btn-sm">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
